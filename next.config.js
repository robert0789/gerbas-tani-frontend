/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['tailwindui.com', 'images.unsplash.com', 'purepng.com', 'drive.google.com']
    },
  }

module.exports = nextConfig